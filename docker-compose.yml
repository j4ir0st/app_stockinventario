services:
  app-stock-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: app-stock-frontend
    ports:
      - "8007:80"
    restart: always
    environment:
      - API_URL=${API_URL}
      - ENTORNO=${ENTORNO}
    networks:
      - stock-network

networks:
  stock-network:
    name: stock-network
    driver: bridge
